# Local compose defaults
# Keep request-origin mode enabled unless you explicitly need to hardcode a broker URL.
MESHBOARD_ALLOW_INSECURE_BROKER_URL=true
MESHBOARD_USE_REQUEST_ORIGIN_BROKER_URL=true
MESHBOARD_BROKER_PATH=/mqtt
MESHBOARD_PUBLIC_BROKER_URL=

# Cloudflare Tunnel
CF_TUNNEL_TOKEN=
